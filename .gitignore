.bundle
db/*.sqlite3
log/*.log
tmp/
.sass-cache/
.project
config/database.yml

# when the assets are compiled locally, ignore them
public/assets/

# application configs need to stay out of repo
config/application.yml
