<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Campus" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <%= link_to "Campus", static_pages_home_path, :class => "brand" %>
          <div class="container nav-collapse">
              <% if user_signed_in? %>
              <div class="btn-group"><%= link_to "Courses",
                                                 courses_path,
                                                 :class => 'btn dropdown-toggle' %></div>
              <div class="btn-group"><%= link_to "Enrollments",
                                                 enrollments_path,
                                                 :class => 'btn dropdown-toggle' %></div>
              <div class="btn-group"><%= link_to "Study Plans",
                                                 study_plans_path,
                                                 :class => 'btn dropdown-toggle' %></div>
              <div class="btn-group"><%= link_to "Wall",
                                                 wall_path,
                                                 :class => 'btn dropdown-toggle' %></div>
              <div class="btn-group"><%= link_to my_study_plans,
                                                 :class => 'btn dropdown-toggle' %></div>
              <div class="btn-group"><%= link_to 'Edit profile', 
                                                 edit_user_registration_path, 
                                                 :class => 'btn dropdown-toggle', 
                                                 :style => 'color: blue' %></div>
              <div class="btn-group"><%= link_to "Logout", 
                                                 destroy_user_session_path,
                                                 method: :delete,
                                                 :class => 'btn dropdown-toggle',
                                                 :style => 'color: red' %></div>
              <% if can? :manage, :all %><li><%= link_to "Admin", admin_root_path %></li><% end %>
              <% elsif !user_signed_in? %>
              <div class="btn-group"><%= link_to "Login",
                                                 new_user_session_path,
                                                 :class => 'btn dropdown-toggle' %></div>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </div>

    <div class="container">
        <div class="row">
          <div class="span9">
            <%= bootstrap_flash %>
            <%= yield %>
          </div>
          <div class="span3">
						<%= yield :sidebar%>
          </div><!--/span-->
        </div><!--/row-->

      <footer>
        <p>&copy; Ruby Community College 2013</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
